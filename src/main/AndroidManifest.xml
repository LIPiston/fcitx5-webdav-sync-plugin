<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 访问 Fcitx5-Android 提供的远程 IPC Service -->
    <uses-permission android:name="${mainApplicationId}.permission.IPC" />
    <!-- WebDAV 网络访问 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name">

        <!-- WebDAV 同步后台服务，由 Fcitx5-Android 绑定 -->
        <service
            android:name=".WebDavSyncService"
            android:directBootAware="true"
            android:exported="true"
            android:permission="${mainApplicationId}.permission.PLUGIN"
            tools:targetApi="24">
            <intent-filter>
                <action android:name="${mainApplicationId}.plugin.SERVICE" />
            </intent-filter>
        </service>

        <!-- 简单的设置界面，可从系统应用列表或 Fcitx5 主应用中打开 -->
        <activity
            android:name=".SettingsActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 提供给 Fcitx5-Android 用于发现插件的占位 Activity -->
        <activity
            android:name=".AboutActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="${mainApplicationId}.plugin.MANIFEST" />
            </intent-filter>
        </activity>
    </application>

</manifest>
